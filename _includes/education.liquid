<!-- _includes/education.liquid -->
<h2 id="education">Education</h2>
<div class="timeline-section">
  <div class="timeline">

    <!-- University of Minnesota -->
    <div class="timeline-item is-current">
      <div class="timeline-dot"></div>
      <div class="timeline-top">
        <div class="timeline-headline">
          <a class="timeline-org" href="#">University of Minnesota</a>
          <span class="badge-current">current</span>
        </div>
        <span class="timeline-role">Ph.D. Student, Computer Science</span>
      </div>
      <div class="timeline-subline">
        <span class="timeline-loc">Minneapolis, MN</span>
        <span class="timeline-period">Sep 2026 &ndash; Present</span>
      </div>
    </div>

    <!-- Kookmin University -->
    <div class="timeline-item">
      <div class="timeline-dot"></div>
      <div class="timeline-top">
        <div class="timeline-headline">
          <a class="timeline-org" href="#">Kookmin University</a>
        </div>
        <span class="timeline-role">B.S., Computer Science and Engineering</span>
      </div>
      <div class="timeline-subline">
        <span class="timeline-loc">Seoul, South Korea</span>
        <span class="timeline-period">Mar 2015 &ndash; Feb 2019</span>
      </div>
      <div class="timeline-badges">
        <span class="badge-minor">Minor in Automotive Software Design</span>
        <span class="badge-honor">Graduated Summa Cum Laude (ranked 1st)</span>
      </div>
    </div>

  </div>
</div>

<style>
.timeline-section { margin-bottom: 2rem; }

.timeline { position: relative; padding-left: 1.75rem; }
.timeline::before {
  content: "";
  position: absolute;
  left: 0.3rem;
  top: 0.4rem;
  bottom: 0.4rem;
  width: 1.5px;
  background: var(--global-divider-color);
}

.timeline-item { position: relative; padding-bottom: 1.9rem; }
.timeline-item:last-child { padding-bottom: 0; }

.timeline-dot {
  position: absolute;
  left: -1.75rem;
  top: 0.2rem;
  width: 11px;
  height: 11px;
  border-radius: 50%;
  background: var(--global-bg-color);
  border: 2px solid var(--global-text-color-light);
}
.timeline-item.is-current .timeline-dot {
  border-color: var(--global-theme-color);
  background: var(--global-theme-color);
  box-shadow: 0 0 0 4px rgba(14, 110, 99, 0.15);
}
.timeline-item.is-current .timeline-period { color: var(--global-theme-color); }

.timeline-top {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  gap: 0.75rem;
  flex-wrap: wrap;
  margin-bottom: 0.2rem;
}
.timeline-headline { display: flex; align-items: center; gap: 0.5rem; flex-wrap: wrap; }
.timeline-org {
  font-size: 1.1rem;
  font-weight: 700;
  color: var(--global-text-color);
  text-decoration: none;
}
.timeline-org:hover { color: var(--global-theme-color); }
.timeline-role { font-size: 0.95rem; font-weight: 700; color: var(--global-theme-color); white-space: nowrap; }

.timeline-subline {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  gap: 0.6rem;
  flex-wrap: wrap;
  margin: 0.1rem 0 0.5rem;
}
.timeline-loc { font-size: 0.85rem; color: var(--global-text-color-light); }
.timeline-period { font-family: monospace; font-size: 0.8rem; color: var(--global-text-color-light); white-space: nowrap; }

.timeline-badges { display: flex; flex-direction: column; align-items: flex-start; gap: 0.4rem; margin-bottom: 0.5rem; }
.badge-minor,
.badge-honor {
  display: inline-block;
  font-family: monospace;
  font-size: 0.7rem;
  letter-spacing: 0.03em;
  padding: 0.2rem 0.6rem;
  border-radius: 4px;
}
.badge-minor { color: var(--global-theme-color); background: rgba(14, 110, 99, 0.1); }
.badge-honor { color: #b8842c; background: #f3e6cd; }
.badge-current {
  display: inline-block;
  font-family: monospace;
  font-size: 0.68rem;
  letter-spacing: 0.04em;
  color: var(--global-theme-color);
  background: rgba(14, 110, 99, 0.1);
  padding: 0.15rem 0.5rem;
  border-radius: 4px;
}

@media (max-width: 576px) {
  .timeline-subline { flex-direction: column; gap: 0.15rem; }
}
</style>
